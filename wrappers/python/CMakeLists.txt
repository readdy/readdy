INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS} ${PROJECT_SOURCE_DIR}/include)

PYTHON_ADD_MODULE(simulation PySimulation.cpp)
TARGET_LINK_LIBRARIES(simulation ${READDY_LIBRARY_NAME} ${READDY_HEADERS} ${READDY_LIBRARIES})