# add READDY_WITH_PYTHON define
ADD_DEFINITIONS(-DREADDY_WITH_PYTHON)

LIST(APPEND SIMULATION_SOURCES PySimulation.cpp)
LIST(APPEND SIMULATION_SOURCES PyPotential.h)
LIST(APPEND SIMULATION_SOURCES PyPotential.cpp)
LIST(APPEND SIMULATION_SOURCES interpreter_lock.h)
LIST(APPEND SIMULATION_SOURCES interpreter_lock.cpp)
LIST(APPEND SIMULATION_SOURCES PyObservable.h)
LIST(APPEND SIMULATION_SOURCES PyObservable.cpp)
LIST(APPEND SIMULATION_SOURCES PyFunction.h)

PYTHON_ADD_MODULE(simulation ${SIMULATION_SOURCES})
TARGET_INCLUDE_DIRECTORIES(simulation PUBLIC $ENV{PYTHON_INCLUDE_DIR})
TARGET_INCLUDE_DIRECTORIES(simulation PUBLIC "${ReaDDy_INCLUDE_DIRS};${Boost_INCLUDE_DIRS}")
TARGET_LINK_LIBRARIES(simulation ${PYTHON_LIBRARIES} ${Boost_LIBRARIES} ${READDY_DEPENDENT_LIBRARIES} ReaDDy)

SET_TARGET_PROPERTIES(simulation
    PROPERTIES
    ARCHIVE_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
    LIBRARY_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
)