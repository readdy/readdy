# add READDY_WITH_PYTHON define
ADD_DEFINITIONS(-DREADDY_WITH_PYTHON)

LIST(APPEND COMMON_MODULE_SOURCES PyConverters.h)
LIST(APPEND COMMON_MODULE_SOURCES PyPotential.h)
LIST(APPEND COMMON_MODULE_SOURCES PyPotential.cpp)
LIST(APPEND COMMON_MODULE_SOURCES interpreter_lock.h)
LIST(APPEND COMMON_MODULE_SOURCES interpreter_lock.cpp)
LIST(APPEND COMMON_MODULE_SOURCES PyObservable.h)
LIST(APPEND COMMON_MODULE_SOURCES PyObservable.cpp)
LIST(APPEND COMMON_MODULE_SOURCES PyFunction.h)

# --- api module ---

LIST(APPEND API_SOURCES api/PySimulation.cpp)

PYTHON_ADD_MODULE(api ${COMMON_MODULE_SOURCES} ${API_SOURCES})
TARGET_INCLUDE_DIRECTORIES(api PUBLIC $ENV{PYTHON_INCLUDE_DIR})
TARGET_INCLUDE_DIRECTORIES(api PUBLIC ${NUMPY_INCLUDE_DIR})
TARGET_INCLUDE_DIRECTORIES(api PUBLIC "${ReaDDy_INCLUDE_DIRS};${Boost_INCLUDE_DIRS}")
TARGET_LINK_LIBRARIES(api ${PYTHON_LIBRARIES} ${Boost_LIBRARIES} ${READDY_DEPENDENT_LIBRARIES} ReaDDy)

SET_TARGET_PROPERTIES(api
    PROPERTIES
    ARCHIVE_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
    LIBRARY_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
)

# --- prototyping module ---

LIST(APPEND PROTOTYPING_SOURCES prototyping/PrototypingModule.cpp)
LIST(APPEND PROTOTYPING_SOURCES prototyping/KernelWrap.h)
LIST(APPEND PROTOTYPING_SOURCES prototyping/Programs.cpp)

PYTHON_ADD_MODULE(prototyping ${COMMON_MODULE_SOURCES} ${PROTOTYPING_SOURCES})
TARGET_INCLUDE_DIRECTORIES(prototyping PUBLIC $ENV{PYTHON_INCLUDE_DIR})
TARGET_INCLUDE_DIRECTORIES(prototyping PUBLIC ${NUMPY_INCLUDE_DIR})
TARGET_INCLUDE_DIRECTORIES(prototyping PUBLIC "${ReaDDy_INCLUDE_DIRS};${Boost_INCLUDE_DIRS}")
TARGET_LINK_LIBRARIES(prototyping ${PYTHON_LIBRARIES} ${Boost_LIBRARIES} ${READDY_DEPENDENT_LIBRARIES} ReaDDy)

SET_TARGET_PROPERTIES(prototyping
        PROPERTIES
        ARCHIVE_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
        LIBRARY_OUTPUT_DIRECTORY "${PYTHON_CPP_MODULES_OUTPUT_DIR}"
        )
