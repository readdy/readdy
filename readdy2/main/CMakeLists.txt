file (GLOB READDY_MAIN_SOURCES *.cpp)
# create library
ADD_LIBRARY(${READDY_LIBRARY_NAME} STATIC ${READDY_MAIN_SOURCES})
# link
TARGET_LINK_LIBRARIES(${READDY_LIBRARY_NAME} ${READDY_LIBRARIES})
# use c++11
set_property (TARGET ${READDY_LIBRARY_NAME} PROPERTY COMPILE_FLAGS -std=c++11)